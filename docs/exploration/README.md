# Exploration

Ideas and approaches that have been researched but not committed to. These are not decisions — they're options worth revisiting when the time is right.

If something here gets adopted, write an ADR in `docs/decisions/` and remove or link the exploration doc.
