apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: ${APP_NAME}
  namespace: flux-system
spec:
  interval: 5m
  sourceRef:
    kind: GitRepository
    name: ${APP_NAME}
  path: ${DEPLOY_PATH}
  prune: true
  wait: true
  timeout: 5m
  targetNamespace: ${NAMESPACE}
