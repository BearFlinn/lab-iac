apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: registry-data
  namespace: registry
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: nfs-client  # Use NFS storage from tower-pc
  resources:
    requests:
      storage: 50Gi
