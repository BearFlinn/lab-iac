apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: feedback-ingest
  namespace: flux-system
spec:
  interval: 1m
  url: https://github.com/grizzly-endeavors/feedback-ingest
  ref:
    branch: main
  provider: github
  secretRef:
    name: github-app-credentials
