apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: hello-cluster
  namespace: flux-system
spec:
  interval: 1m
  url: https://github.com/Grizzly-Endeavors/hello-cluster
  ref:
    branch: main
  provider: github
  secretRef:
    name: github-app-credentials
