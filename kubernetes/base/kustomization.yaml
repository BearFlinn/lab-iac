apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - registry
  - github-runner
  - postgresql
  - garage
  # ingress-nginx and nfs-provisioner managed via Helm
  # See kubernetes/ingress-nginx/README.md and kubernetes/nfs-provisioner/values.yaml
