apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: argo-minio-credentials
  namespace: argo
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: openbao
  target:
    name: argo-minio-credentials
    creationPolicy: Owner
  data:
    - secretKey: accesskey
      remoteRef:
        key: grizzly-platform/cicd/argo-minio
        property: access_key
    - secretKey: secretkey
      remoteRef:
        key: grizzly-platform/cicd/argo-minio
        property: secret_key
