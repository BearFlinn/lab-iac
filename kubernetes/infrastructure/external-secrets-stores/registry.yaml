apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: registry-s3-creds
  namespace: registry
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: openbao
  target:
    name: registry-s3-creds
    creationPolicy: Owner
  data:
    - secretKey: accessKey
      remoteRef:
        key: lab-iac/stores/minio-bulk
        property: root_user
    - secretKey: secretKey
      remoteRef:
        key: lab-iac/stores/minio-bulk
        property: root_password
