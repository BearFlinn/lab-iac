---
# Kubernetes Control Plane - Quick Reference Inventory
# Use for control-plane-only operations
# All host variables are inherited from all-nodes.yml
#
# This inventory contains only connection details for quick targeting.
# For complete host configuration, see ansible/inventory/all-nodes.yml

all:
  children:
    k8s_control_plane:
      hosts:
        dell-inspiron-15:
          ansible_host: 10.0.0.226
          ansible_connection: ssh
          ansible_user: bearf

    k8s_cluster:
      children:
        k8s_control_plane:
