# Managed by Ansible — r730xd-prometheus role
# ingress-nginx controller metrics exposed via NodePort (Phase 6)
- targets:
    - "{{ hostvars[groups['k8s_control_plane'][0]]['ansible_host'] }}:30889"
  labels:
    env: "grizzly-platform"
    instance: "ingress-nginx-controller"
    component: "ingress-nginx"
