# Managed by Ansible — r730xd-prometheus role
# ARC v2 controller metrics exposed via NodePort (Phase 5)
- targets:
    - "{{ hostvars[groups['k8s_control_plane'][0]]['ansible_host'] }}:30885"
  labels:
    env: "grizzly-platform"
    instance: "arc-controller"
    component: "arc-controller"
